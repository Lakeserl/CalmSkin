package com.lakeserl.promotion_service.enums;

/** Kind of promotion. Drives how the engine loads and applies it. */
public enum PromotionType {
    VOUCHER,
    FLASH_SALE,
    BUNDLE,
    PRODUCT_DISCOUNT,
    CATEGORY_DISCOUNT,
    FREE_GIFT
}
