spring:
  config:
    activate:
      on-profile: prod
  jpa:
    show-sql: false

logging:
  level:
    root: INFO
    com.lakeserl.inventory_service: INFO

management:
  tracing:
    sampling:
      probability: 0.1
